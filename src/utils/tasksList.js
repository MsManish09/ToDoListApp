
// to store the list of tasks
export const tasksArray = []

// create useState and store the tasksArray for automatic rendering when change in list
 export const [tasksListState, setTasksListState] = useState(tasksArray)

// export default {tasksArray, task}

//Store the tasksListState in local storage.
